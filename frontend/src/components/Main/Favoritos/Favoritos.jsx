import React from "react";

const Favoritos = () => {
  return <div className="favoritos-container">
      <h1 className="favoritos-title">Favoritos</h1>
      {/* <Search pueblos={pueblos} onPuebloChange={handlePuebloChange} /> */}

      {/* {loading ? (
        <span className="favoritos-loading"><span className="loading-text"><RotatingLines
              visible={true}
              height="96"
              width="96"
              color="#8C6A43"
              strokeWidth="5"
              animationDuration="0.75"
              ariaLabel="rotating-lines-loading"
              wrapperStyle={{}}
              wrapperClass=""
              /></span></span>
      ) : (
        <EventosList eventos={eventos} />
      )} */}
    </div>
};

export default Favoritos;
